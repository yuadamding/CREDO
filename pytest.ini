[pytest]
markers =
    unit: small unit tests
    semantic: model and finite-measure semantic invariants
    integration: integration tests
    runner: command-line runner smoke tests
    biology: biology analysis tests
    slow: slow tests excluded from default CI
    gpu: tests requiring a GPU
    distributed: distributed or multi-device tests
    randomized: randomized stress tests
